1、TCP乱序到达如何处理
2、select、epoll;epoll的使用，底层实现
3、STL使用
4、hash冲突解决
5、STL map
6、GDB使用
7、cpu过高如何处理
8、fork
答：子进程是父进程的副本。子进程获得父进程的数据空间、堆和栈的副本。父进程和子进程并不共享这些存储空间的部分。父进程和子进程共享正文段。传统的fork调用时直接把所有资源复制给新创建的进程，这样的操作很低效。现代linux使用的是写时复制（Copy on Write）技术，内核不必要复制整个地址空间，而是让父子进程共享一个地址空间，当需要写入的时候才会去复制定制空间。
